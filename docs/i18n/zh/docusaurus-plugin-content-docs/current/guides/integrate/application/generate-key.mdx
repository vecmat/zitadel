import ThemedImage from "@theme/ThemedImage";

export default function GenerateKey(props) { return props.appType == "api" || props.authType == "jwt" ? (
    <div>
      <p>
        当您成功创建您的应用程序使用身份验证类型
        JWT 后，您可以在
        的配置部分和 Keys 卡中创建密钥，例如下面的图像显示：
      </p>
      <img
        width="400px"
        alt="生成密钥"
        src="/img/guides/application/generate-key.png"
      />
    </div>
  ) : null; }
